# 工单管理 — 完整功能介绍

> AI 安全管理平台核心子模块，承载"任务流转与处置闭环"能力。
> 三方协同：物业（发起+验收）、消防服务商（处置执行）、安全监管中心（监管督办）。

---

## 一、功能全景总览

```
工单管理
├── 1. 流程模板编排        ← 定义工单类型、表单字段、流转规则
├── 2. 工单发起            ← 选模板 → 填表单 → 提交创建
├── 3. 工单监控列表        ← 全量工单查看、多维度筛选、SLA 预警
├── 4. 工单详情与处置       ← 流程进度、只读信息、节点操作、流转记录
├── 5. 动态表单渲染        ← 模板字段运行时渲染，三态权限控制
├── 6. SLA 计时与预警       ← TTR/TTS 双计时、绿黄红三色预警
├── 7. 节点指派机制        ← 静态指派 / 指派给发起人 / 动态指派
├── 8. 跨企业流转           ← 父子工单接力、跨主体数据权限
├── 9. 按钮权限体系        ← 工单级 × 节点级，按角色+状态控制
├── 10. 工单归档            ← 已关闭工单检索、导出
├── 11. 统计看板            ← SLA 达标率、工单趋势、效率排行
└── 12. 消息通知            ← 站内消息 + 语音通知 + 短信通知
```

---

## 二、各功能详细介绍

### 1. 流程模板编排

**是什么**：管理员通过可视化界面定义工单模板，包括节点流程、表单字段、SLA 配置和流转规则。模板发布后，业务人员按模板创建工单实例。

**已实现的功能**：

| 功能点 | 说明 |
|--------|------|
| 模板列表（TemplateList） | 模板 CRUD + 状态管理（发布/停用/编辑/删除） |
| 三步配置（TemplateConfig） | 基础设置 → 表单设计（FormDesigner）→ 流程设计（FlowDesigner） |
| 6 类流程节点 | 发起（start）/ 指派（assign）/ 执行（execute）/ 确认（confirm）/ 关闭（close）/ 自定义（custom） |
| 节点字段配置 | 每个节点可配置独立的表单字段，支持 12 种字段类型 |
| SLA 计时配置 | 按优先级预设 TTR/TTS 时限，黄灯阈值可自定义 |
| 流转规则 | 顺序流转 + 操作结果分支 + 组织属性分支 |
| 指派策略 | 按岗位 / 按人员 / 按角色 / 按部门 / 企业外人员 |
| 条件分支（v1） | 基于操作结果（通过/驳回/转单）和组织属性（部门/岗位）自动路由 |
| 自定义节点 | 当 5 种预设类型无法覆盖时，可创建自定义节点 |
| 字段来源标记 | manual（手动）/ auto（系统自动）/ inherited（上游继承）/ callback（外部回调） |

**待实现**：模板版本管理（发布/停用/历史版本）、表单字段值分支（v2）、并行子流程（v2）。

**关键页面**：
- `/system/template` — 模板列表页
- `/system/template/config/:id?` — 模板配置页（三步向导）

---

### 2. 工单发起

**是什么**：物业管理员从工作台快捷入口发起工单，选择已发布的流程模板，填写基本信息和模板定义的表单字段，提交后生成工单实例。

**入口**：工作台 `QuickActionsWidget` → `CreateOrderDialog`（抽屉模式，800px）。

**已实现的功能**：

| 功能点 | 说明 |
|--------|------|
| 模板选择 | el-select 下拉选择已发布模板，支持 filterable 搜索 |
| 基本信息填写 | 工单标题（必填）+ 优先级（低/普通/高/紧急，默认"普通"）+ 备注（选填） |
| 动态表单渲染 | 选中模板后，DynamicForm 渲染模板发起节点的可编辑字段 |
| 提交创建 | 校验 → 提交 → 生成草稿工单，刷新列表 |
| 切换模板 | 选中模板后可切换，切换后重新加载表单字段 |

**业务规则**：
- 仅展示"已发布"状态的模板
- 未选模板时"发起"按钮 disabled
- 创建后状态固定为"草稿"，进度取决于模板节点数
- 工单编号由系统自动生成（格式 `WO{YYYYMMDD}-{序号}`）

**关键组件**：`CreateOrderDialog.vue`

---

### 3. 工单监控列表

**是什么**：工单全量列表页，面向管理角色（消防安全责任人、项目负责人、安全监管员），提供统计卡片、多维度筛选、SLA 三色预警和数据表格。

**已实现的功能**：

| 功能点 | 说明 |
|--------|------|
| 统计卡片 × 4 | 全部 + 草稿 + 进行中 + 已关闭，点击卡片快速筛选，计数由后端 stats 接口驱动 |
| 关键词搜索 | 模糊匹配工单编号、发起人姓名，300ms 防抖 |
| 多维度筛选 | 模板名称下拉 + 优先级下拉 + SLA 状态下拉 + 创建时间日期范围（快捷选项：今天/本周/本月） |
| SLA 三色预警 | 🟢 正常（进度 < 80%）/ 🟡 预警（80%~100%）/ 🔴 超时（≥100%），含剩余时间文案 |
| SLA 详情 Tooltip | 悬浮 SLA 列展示 TTR/TTS 各自进度、时限值、黄灯阈值 |
| 数据表格 | 工单编号 + 模板名称 + 工单进度（如 3/5）+ 当前节点 + 状态标签 + 优先级 + SLA 状态 + 当前处理人 + 发起人 + 创建时间 |
| 超时高亮 | 红灯工单置顶 + 浅红色背景行 |
| 分页 | 默认每页 20 条，支持 10/20/50/100 |
| URL 参数同步 | 筛选条件反映在 URL query 中，支持分享链接 |
| 岗位数据范围过滤 | 根据当前登录角色自动过滤可见工单范围 |

**关键页面**：`/system/monitor` — `WorkOrderMonitor.vue`

---

### 4. 工单详情与处置

**是什么**：工单详情独立页面，面向处理人和管理者。未关闭工单采用双栏布局（左：流程进度 + 只读信息 + 当前节点操作；右：流转记录时间线），已关闭工单为简化只读模式。

**已实现的功能**：

| 功能点 | 说明 |
|--------|------|
| 页面头部 | 优先级 Tag + 工单标题 + 编号/模板/发起人 + 上一条/下一条切换 |
| 流程进度 | 动态渲染模板流程节点（过滤 condition/external），蓝=已完成/当前，灰=待处理，步骤可点击定位 |
| 工单信息（只读区） | 聚合已完成节点的提交数据，label-value 对展示，图片显示缩略图 |
| 当前节点操作（可编辑区） | 当前节点名称为标题，仅展示权限为 editable 的字段 + DynamicForm + 操作按钮 |
| 流转记录时间线 | 右栏 361px，操作记录按时间倒序，含操作标题+时间+操作人+描述 |
| 工单级操作 | 强制改派 + 取消工单（状态 ≠ closed 时显示） |
| 节点级操作 | 接单（assign 节点+抢单模式+无人认领时）/ 通过/驳回（confirm 节点，按模板 actions 配置） |
| 已关闭简化模式 | 基本信息 + SLA 指标卡片 + 全部节点只读展示 |
| 响应式 | 屏幕宽度 < 1280px 时右栏流转记录可折叠 |

**关键页面**：`/system/order/:id` — `WorkOrderDetail.vue`

**关键组件**：`OrderProgress.vue`（流程进度）、`FlowRecords.vue`（流转记录）、`DynamicForm.vue`（动态表单）

---

### 5. 动态表单渲染（DynamicForm）

**是什么**：平台基础设施，将模板中配置的表单字段在运行时渲染为可交互表单，按节点权限控制可编辑/只读/隐藏。

**三态权限控制**：

| mode | 运行时行为 | 视觉表现 |
|------|-----------|---------|
| `hidden` | 不渲染（v-if="false"） | 不可见 |
| `readonly` | 渲染控件但 disabled + 灰色背景 | 可见，值回填，不可操作 |
| `editable` | 正常交互 | 正常输入控件 |

**8 种字段类型映射**：

| 类型 | 渲染控件 |
|------|---------|
| `input` | el-input |
| `textarea` | el-input type="textarea" |
| `radio` | el-radio-group |
| `checkbox` | el-checkbox-group |
| `select` | el-select |
| `date` | el-date-picker |
| `upload` | el-upload |
| `number` | el-input-number |

**组件接口**：
```typescript
interface Props {
  fields: FormField[]           // 模板定义的所有字段
  permissions: FieldPermission[] // 当前节点的字段权限配置
  initialData?: Record<string, any>  // 已有表单数据（回填）
  readonly?: boolean            // 全局只读模式
}

interface Emits {
  'submit': [data: Record<string, any>]
}
```

**使用场景**：

| 场景 | 载体 | 说明 |
|------|------|------|
| 发起工单 | `el-drawer` | 嵌入 CreateOrderDialog，渲染发起节点可编辑字段 |
| 执行处置 | 详情页操作区 | 渲染当前节点 editable 字段，其余只读 |
| 验收确认 | 详情页操作区 | 与执行复用，按 nodeType 切换权限 |
| 详情查看 | 详情页只读区 | 未关闭时可查看历史数据，已关闭时全只读 |

**关键组件**：`components/business/DynamicForm.vue`

---

### 6. SLA 计时与预警

**是什么**：对工单处理时效进行双指标管控，通过绿黄红三色预警帮助管理者及时发现超时风险。

**双计时模型**：

| 指标 | 全称 | 起终点 | 用途 |
|------|------|--------|------|
| **TTR** | 响应时限 | 任务到达 → 接单 | 衡量响应速度 |
| **TTS** | 解决时限 | 接单 → 关闭 | 端到端时效考核 |

> 无指派节点的工单：仅 TTS，起点下沉为执行节点到达。

**三色预警规则**：

| 状态 | 颜色 | 条件 | 含义 |
|------|------|------|------|
| 正常 | 🟢 `#52C41A` | 进度 < 80% | 时间充裕 |
| 预警 | 🟡 `#FAAD14` | 80% ≤ 进度 < 100% | 即将超时 |
| 超时 | 🔴 `#FF4D4F` | 进度 ≥ 100% | 已超时 |

> 进度 = 已用时间 / SLA时限 × 100%

**关键规则**：
- 驳回不重置 TTS，TTS 继续累计
- 转单不重置 TTS，TTS 继续累计
- 草稿和已关闭状态不展示 SLA 状态（显示"—"）
- SLA 进度每 30 秒自动刷新

**优先级与默认时限**：

| 优先级 | TTR 默认 | TTS 默认 | 典型场景 |
|--------|---------|---------|---------|
| 紧急 | 15 分钟 | 2 小时 | 火警核实、安全事件 |
| 高 | 30 分钟 | 8 小时 | 重要设备故障、限期整改 |
| 普通 | 2 小时 | 24 小时 | 设备报修、常规巡检 |
| 低 | 8 小时 | 72 小时 | 台账更新、计划保养 |

**SLA 升级动作**：抄送通知 + 自动转派 + 退回上级（按配置触发）。

---

### 7. 节点指派机制

**是什么**：决定"这个节点由谁处理"的三种范式，覆盖企业内固定人员、发起人本人、跨企业动态指定等场景。

**三种指派范式**：

| 范式 | 决定时机 | 决定者 | 适用场景 |
|------|---------|--------|---------|
| **静态指派** | 模板配置时 | 模板设计者 | 企业内流程，处理人已知且固定 |
| **指派给发起人** | 模板配置时（选择） | 模板设计者 | 谁发起谁处理/验收 |
| **动态指派** | 工单运行时 | 工单当前操作者 | 跨企业流程，处理人需接收方自行指定 |

**静态指派 — 多人模式**：

| 模式 | 含义 | SLA 起点 |
|------|------|---------|
| `anyone` | 任一人接单（抢单），先接先得 | 任务进入公海池时间 |
| `each` | 每人各自生成独立任务 | 各自任务到达时间 |

**动态指派典型场景**（跨企业督办）：
```
监管方创建督办模板 → 配置指派节点 assignSource='dynamic'
→ 绑定表单字段 'rectificationOwner'
→ 预警方管理员在运行时从本企业人员中选择整改负责人
→ 系统自动指派
```

**转单机制**：
- 权限：当前执行人、部门负责人均可发起
- 范围：限同部门/同岗位
- SLA：计时累积，不重置

---

### 8. 跨企业流转

**是什么**：工单在不同企业之间流转的机制，通过父子工单接力实现数据隔离与双向同步。

**接力模型**：

```
监管方企业                          预警方企业
─────────                          ──────────
① 发起（隐患信息+单位信息自动填充）  ② 指派（动态指派，管理员选负责人）
④ 确认（审核整改结果）               ③ 执行（整改执行+拍照上传）
⑤ 关闭（归档+回调预警系统）
```

**父子工单机制**：
- 监管方提交父工单 → 系统自动在预警方企业生成子工单
- 父→子同步：整改内容、标准、期限（只读）
- 子→父回传：整改结果、照片、完成时间
- 驳回时父工单驳回 → 子工单重新打开

**跨企业数据权限**：

| 阶段 | 监管方可见 | 预警方可见 |
|------|-----------|-----------|
| 发起时 | 全部（含内部备注） | 不可见 |
| 预警方处理中 | 工单状态、处理人 | 督办内容、整改要求（不含监管方内部备注） |
| 整改结果回流后 | 整改结果、证据照片 | 已提交结果 |
| 审核驳回后 | 驳回原因 | 驳回原因 + 需继续整改 |
| 关闭归档后 | 全部归档数据 | 本企业相关数据 |

---

### 9. 按钮权限体系

**是什么**：按"状态 × 角色 × 当前节点"控制页面按钮的显示与隐藏，确保不同角色在工单不同阶段只能看到自己能操作的按钮。

**四种虚拟角色**：

| 角色 | 判定方式 |
|------|---------|
| **发起人** | 当前用户 = 工单创建者 |
| **节点负责人** | 当前用户 = 当前节点的负责人 |
| **管理员** | 系统全局标记（平台管理员） |
| **查看者** | 以上都不是 |

**权限按作用域分离**：

| 层级 | 按钮 | 显示条件 | 位置 |
|------|------|---------|------|
| **工单级** | 取消工单 | `status !== 'closed'` | 底部操作栏 |
| **工单级** | 强制改派 | `status === 'active' && currentAssigneeId != null` | 底部操作栏 |
| **工单级** | 修改优先级 | `status !== 'closed'` | 顶部 |
| **节点级** | 接单 | assign 节点 + `assignConfig.multipleMode === 'anyone'` + 无人认领 | 节点操作卡片 |
| **节点级** | 通过/驳回 | confirm 节点，由模板 `flowNode.actions` 配置 | 节点操作卡片 |
| **节点级** | 保存（处置） | execute 节点，节点负责人 | 节点操作卡片 |

**实现**：`src/utils/permission.ts`（角色判定）+ `src/composables/useButtonPerms.ts`（布尔权限对象）。

---

### 10. 工单归档（P1，待实现）

**是什么**：已关闭工单的独立检索页，支持高级筛选和导出。

| 功能点 | 说明 |
|--------|------|
| 归档列表 | 复用 filter-bar 范式，默认展示已关闭 |
| 高级筛选 | 时间范围 + 模板 + 优先级 + SLA 结果 |
| 导出 | Excel/CSV |
| 详情查看 | 复用 WorkOrderDetail 独立页 |

**关键页面**（待建）：`WorkOrderArchive` — `/workflow/archive`

---

### 11. 统计看板（P1，待实现）

**是什么**：工单数据分析仪表盘，通过图表展示 SLA 达标率、工单趋势和处理人效率。

| 功能点 | 说明 |
|--------|------|
| 顶部指标卡 | 本月总数 / 达标率 / 平均时长 / 超时数 |
| 工单趋势图 | 近 30 天折线图 |
| 模板分布饼图 | 按模板类型统计工单分布 |
| 处理人效率排行表 | 按处理人统计平均处置时长 |
| 时间范围筛选器 | 支持自定义时间范围 |

**关键页面**（待建）：`WorkOrderStats` — `/system/dashboard`

---

### 12. 消息通知（P2，待实现）

**是什么**：工单事件通知系统，覆盖 SLA 预警和工单指派的双端通知。

| 功能点 | 说明 |
|--------|------|
| 站内消息中心 | Web 顶部铃铛 + 消息列表 |
| 语音通知 | SLA 红灯时自动拨打 |
| 短信通知 | 紧急工单指派时发送 |

---

## 三、核心数据模型

### 工单实例（WorkOrderInstance）

```typescript
interface WorkOrderInstance {
  id: number                    // 主键
  orderNo: string               // 工单编号 WO{YYYYMMDD}-{序号}
  templateId: number            // 关联模板 ID
  templateName: string          // 模板名称
  templateVersion: number       // 模板版本号
  status: 'draft' | 'active' | 'closed'  // 实例状态
  priority: 'urgent' | 'high' | 'normal' | 'low'  // 优先级
  currentNodeId: number         // 当前节点 ID
  currentNodeName: string       // 当前节点名称
  currentNodeType: string       // 当前节点类型
  currentAssigneeId: number | null  // 当前处理人 ID
  currentAssigneeName: string | null // 当前处理人姓名
  creatorId: number             // 发起人 ID
  creatorName: string           // 发起人姓名
  creatorOrgId: number          // 发起人所属企业 ID
  parentOrderId: number | null  // 父工单 ID（跨企业场景）
  createdAt: string             // 创建时间
  updatedAt: string             // 最后更新时间
  closedAt: string | null       // 关闭时间
  closedBy: string | null       // 关闭操作人
  formData: Record<string, any> // 聚合表单数据
  sla: SlaInfo                  // SLA 子对象
}
```

### 实例状态机

| 状态 | 中文 | 判定逻辑 |
|------|------|---------|
| `draft` | 草稿 | 所有节点均为 pending |
| `active` | 进行中 | 部分节点已开始但非全部完成 |
| `closed` | 已关闭 | 所有节点为 completed 或 skipped |

进行中的具体阶段（指派/处置/验收等）通过 `currentNodeName` 体现，由模板配置决定。

---

## 四、接口清单

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取模板列表 | GET | `/api/workflow/templates` | 模板 CRUD |
| 获取模板详情 | GET | `/api/workflow/templates/{id}/detail` | 含 formSchema + flowDefinition |
| 获取工单列表 | GET | `/api/v1/work-order/instances` | 分页 + 多维度筛选 |
| 获取工单详情 | GET | `/api/v1/work-order/instances/:id` | 含节点链路 + 处理记录 |
| 获取工单统计 | GET | `/api/v1/work-order/instances/stats` | 按状态分组计数 |
| 创建工单 | POST | `/api/v1/work-order/instances` | 从模板创建草稿工单 |
| 取消工单 | POST | `/api/v1/work-order/instances/:id/cancel` | 需填写取消原因 |
| 强制改派 | POST | `/api/v1/work-order/instances/:id/reassign` | 需选择目标处理人 |

---

## 五、页面与路由总览

| 页面 | 路由 | 类型 | 端 | 状态 |
|------|------|------|-----|------|
| 模板列表 | `/system/template` | 列表管理 | Web | ✅ 已完成 |
| 模板配置 | `/system/template/config/:id?` | 三步向导 | Web | ✅ 已完成 |
| 工单监控 | `/system/monitor` | 列表管理 | Web | ✅ 已完成 |
| 工单详情 | `/system/order/:id` | 详情+处置 | Web | ✅ 已完成 |
| 工单数据看板 | `/system/dashboard` | 数据看板 | Web | 📝 设计完成 |
| 工单归档 | `/workflow/archive` | 列表+检索 | Web | 📝 待设计 |
| 移动端处置 | 小程序 | 待办+处置 | 微信小程序 | 📝 待实现 |

---

## 六、关键设计决策

1. **"我的工单" vs "工单监控"分离**：个人视角（工作台 MyTasksWidget）与管理视角（独立页面 /system/monitor）是两个不同产品，入口和数据范围均不同。

2. **发起工单入口在工作台**：`QuickActionsWidget → CreateOrderDialog`，工单监控页不提供发起入口，纯监控用途。

3. **3 状态简化**：实例状态从 6 状态简化为 draft/active/closed 3 状态，进行中阶段通过 currentNodeName 体现，使模板可自由配置节点而无需修改状态枚举。

4. **按钮权限按作用域分离**：工单级操作（取消、改派）不关心当前节点类型；节点级操作（接单、通过/驳回）由节点自身配置决定。

5. **SLA 双计时不重置**：驳回和转单不重置 TTS，倒逼全链路高效。

6. **模板独立版本**：已发布模板产生工单后锁定，修改需新建版本，进行中的旧版实例不受影响。

7. **跨企业路由到企业不路由到人**：监管方发起工单时只路由到预警方企业，由预警方管理员在运行时自行分派内部人员。

---

## 七、开发阶段与优先级

| 阶段 | 模块 | 状态 |
|------|------|------|
| 已有 | M0 模板配置 | ✅ 完成 |
| 已有 | M1 工单监控 | ✅ 完成 |
| 已有 | M-N 动态表单渲染 | ✅ 完成 |
| 已有 | M2 工单发起 | ✅ 完成 |
| 已有 | M3 Web 端详情+处置 | ✅ 完成 |
| MVP-2 | M3 移动端处置 | 📝 待实现 |
| MVP-3 | M4 工单归档 | 📝 待实现 |
| MVP-3 | M5 统计看板 | 📝 待实现 |
| 延后 | M6 消息通知 | 📝 待实现 |
